# Uncensored

## Setup Instructions

1. Clone this repository.
2. Install CocoaPods:
   ```
   pod install
   ```
3. Open `Uncensored.xcworkspace` in Xcode.
4. Add your `GoogleService-Info.plist` file to the project (do not version control).
5. Configure Firebase and Google Sign-In.
